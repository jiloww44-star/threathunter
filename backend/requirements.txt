fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
pyyaml>=6.0
pydantic>=2.7
python-multipart>=0.0.9
# Production profile (docker-compose): asyncpg, celery, redis, spacy,
# sentence-transformers, cryptography — see Part 4/9 of the spec.
# Demo profile runs dependency-light by design (spec Part 10).

{
  "name": "logon-assurance-gate",
  "version": "0.1.0",
  "private": true,
  "description": "LOG_ON Governance + Agent Assurance hackathon MVP running its reasoning layer through SERV.",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/policy.test.js test/mcp.test.js",
    "test:mcp": "node --test test/mcp.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "type": "module",
  "dependencies": {
    "@modelcontextprotocol/client": "^2.1.0",
    "@modelcontextprotocol/server": "^2.1.0",
    "zod": "^4.6.5"
  }
}
